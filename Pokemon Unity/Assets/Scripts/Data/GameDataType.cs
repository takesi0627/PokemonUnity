using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class GameDataType {
// all global enum declaration
	public enum EVYeildType
	{
		HP,
		ATK,
		DEF,
		SPA,
		SPD,
		SPE,
	}

	public enum BaseStatsType
	{
		HP,
		ATK,
		DEF,
		SPA,
		SPD,
		SPE,
	}
}
